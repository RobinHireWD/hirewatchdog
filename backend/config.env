DB_USER=postgres          # Your PostgreSQL username
DB_PASSWORD=Robin@123      # Your PostgreSQL password
DB_HOST=localhost          # Database host (usually 'localhost' for local dev)
DB_NAME=hirewatchdog_db    # Your PostgreSQL database name
PORT=5001                 # Port where your Express server will run (default is 5000)
